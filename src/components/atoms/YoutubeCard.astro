---
interface Props {
    src: string
    title?: string
}
const { src, title } = Astro.props

---
<article class="rounded-2xl overflow-hidden">
    <fast-youtube src={src} title={title}></fast-youtube>`
</article>