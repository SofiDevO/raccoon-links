---
interface Props {
    src: string
    title?: string
}
const { src, title } = Astro.props

import YoutubeData from '@data/youtubeChannels.json';



const links = YoutubeData.map(data => data.split("channel/")[1])
console.log(links);

---
